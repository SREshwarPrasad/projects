services:
  - type: web
    name: crop-yield-webapp
    env: python
    plan: free
    rootDir: crop-yield-webapp
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
